{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Terminal terminal-market",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/",
            "cwd": "${workspaceFolder}",
            "env": {
                "GO_ENV": "development"
            }
        }
    ]
}